apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "oncall.fullname" . }}-calculator
  namespace: {{ include "oncall.namespace" . }}
  labels:
    {{- include "oncall.labels" . | nindent 4 }}
    app.kubernetes.io/component: calculator
spec:
  replicas: {{ .Values.calculator.replicaCount }}
  selector:
    matchLabels:
      {{- include "oncall.selectorLabels" (dict "name" "calculator" "release" .Release.Name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "oncall.labels" . | nindent 8 }}
        {{- include "oncall.selectorLabels" (dict "name" "calculator" "release" .Release.Name) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "oncall.serviceAccountName" . }}
      {{- include "oncall.imagePullSecrets" . | nindent 6 }}
      initContainers:
        - name: wait-for-mongodb
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for MongoDB at mongodb:{{ .Values.mongodb.service.port }}..."
              until nc -z mongodb {{ .Values.mongodb.service.port }}; do
                echo "MongoDB not ready — sleeping 3s"
                sleep 3
              done
              echo "MongoDB is reachable"
      containers:
        - name: calculator
          image: "{{ .Values.calculator.image.repository }}:{{ .Values.calculator.image.tag }}"
          imagePullPolicy: {{ .Values.calculator.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.calculator.service.targetPort }}
              protocol: TCP
          env:
            - name: MONGO_URI
              value: {{ .Values.global.mongodb.uri | quote }}
            - name: DB_NAME
              value: {{ .Values.global.mongodb.dbName | quote }}
          resources:
            {{- toYaml .Values.calculator.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      {{- with .Values.calculator.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.calculator.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.calculator.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
